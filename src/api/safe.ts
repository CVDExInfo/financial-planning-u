/**
 * Production API wrapper - NO MOCK DATA
 * All API calls go directly to production endpoints
 * Errors are logged and propagated (no silent fallbacks)
 */

// REMOVED: Mock data support has been eliminated for production
// All API calls now go directly to production Lambda handlers

name: Deploy UIs (PMO + Finanzas)

on:
  workflow_dispatch: {}
  push:
    branches: ["r1-finanzas-dev-wiring", "main"]

permissions:
  id-token: write
  contents: write

env:
  AWS_REGION: ${{ vars.AWS_REGION || 'us-east-2' }}
  # Determine environment based on branch
  DEPLOYMENT_ENV: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
  # S3 and CloudFront configuration - use specific values for production
  S3_BUCKET_NAME: ${{ github.ref == 'refs/heads/main' && 'ukusi-ui-finanzas-prod' || vars.S3_BUCKET_NAME }}
  CLOUDFRONT_DIST_ID: ${{ github.ref == 'refs/heads/main' && 'EPQU7PVDLQXUA' || vars.CLOUDFRONT_DIST_ID }}
  # API configuration - use prod endpoint for main branch, dev for others
  FINZ_API_STACK: ${{ github.ref == 'refs/heads/main' && 'finanzas-sd-api-prod' || (vars.FINZ_API_STACK || 'finanzas-sd-api-dev') }}
  DEV_API_URL: ${{ github.ref == 'refs/heads/main' && 'https://m3g6am67aj.execute-api.us-east-2.amazonaws.com/prod' || (vars.DEV_API_URL || '') }}
  VITE_FINZ_ENABLED: ${{ vars.VITE_FINZ_ENABLED || 'true' }}
  VITE_USE_MOCKS: "false"
  EXPECTED_API_ID: ${{ vars.FINZ_EXPECTED_API_ID || 'm3g6am67aj' }}

jobs:
  build-and-deploy-all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Preflight env
        run: |
          set -euo pipefail
          echo "🚀 Deployment Environment: ${DEPLOYMENT_ENV}"
          echo "📦 S3 Bucket: ${S3_BUCKET_NAME}"
          echo "🌐 CloudFront Distribution: ${CLOUDFRONT_DIST_ID}"
          echo "🔌 API Endpoint: ${DEV_API_URL:-'(not set)'}"
          for k in S3_BUCKET_NAME CLOUDFRONT_DIST_ID; do
            v="${!k:-}"; [ -n "$v" ] || { echo "❌ Missing repo var: $k"; exit 1; }
          done
          printf '%s\n' "VITE_USE_MOCKS=false" >> "$GITHUB_ENV"
          sed -i 's/\r$//' "$GITHUB_ENV"
          echo "✅ Env OK"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: |
          set -e
          echo "Installing frontend dependencies (prefer lock, fallback to dynamic)"
          if ! npm ci; then
            echo "npm ci failed (likely lock mismatch). Falling back to npm install."
            npm install
          fi

      - name: Normalize API base from repo var
        run: |
          set -euo pipefail
          if [ -z "${DEV_API_URL}" ]; then
            echo "⚠️  DEV_API_URL is empty. Using default endpoint based on environment."
            if [ "${DEPLOYMENT_ENV}" = "prod" ]; then
              DEFAULT_URL="https://m3g6am67aj.execute-api.us-east-2.amazonaws.com/prod"
            else
              DEFAULT_URL="https://m3g6am67aj.execute-api.us-east-2.amazonaws.com/dev"
            fi
            printf '%s\n' "VITE_API_BASE_URL=$DEFAULT_URL" >> "$GITHUB_ENV"
            echo "Using default API endpoint: $DEFAULT_URL"
          else
            CLEAN_URL="${DEV_API_URL%/}"
            printf '%s\n' "DEV_API_URL=$CLEAN_URL" >> "$GITHUB_ENV"
            printf '%s\n' "VITE_API_BASE_URL=$CLEAN_URL" >> "$GITHUB_ENV"
            sed -i 's/\r$//' "$GITHUB_ENV"
            echo "Using configured API endpoint: $CLEAN_URL"
          fi

      - name: "Build PMO Portal (base: /)"
        run: |
          echo "Building PMO Portal (base: /)..."
          BUILD_TARGET=pmo npm run build || { echo "❌ PMO build failed"; exit 1; }
          echo "✅ PMO Portal built → dist-pmo/"

      - name: "Build Finanzas SDT Portal (base: /finanzas/)"
        env:
          VITE_API_BASE_URL: ${{ env.VITE_API_BASE_URL }}
          VITE_FINZ_ENABLED: "true"
          VITE_USE_MOCKS: ${{ env.VITE_USE_MOCKS }}
          VITE_PUBLIC_BASE: "/finanzas/"
        run: |
          set -euo pipefail
          printf '%s\n' "VITE_PUBLIC_BASE=/finanzas/" >> "$GITHUB_ENV"
          printf '%s\n' "VITE_FINZ_ENABLED=true" >> "$GITHUB_ENV"
          printf '%s\n' "VITE_USE_MOCKS=false" >> "$GITHUB_ENV"
          
          # Use environment-specific API endpoint
          if [ -n "${VITE_API_BASE_URL:-}" ]; then
            printf '%s\n' "VITE_API_BASE_URL=${VITE_API_BASE_URL%/}" >> "$GITHUB_ENV"
            echo "✅ Using API endpoint: ${VITE_API_BASE_URL}"
          else
            # This should not happen after the previous step, but keep as fallback
            if [ "${DEPLOYMENT_ENV}" = "prod" ]; then
              printf '%s\n' "VITE_API_BASE_URL=https://m3g6am67aj.execute-api.us-east-2.amazonaws.com/prod" >> "$GITHUB_ENV"
              echo "⚠️  Fallback to prod API endpoint"
            else
              printf '%s\n' "VITE_API_BASE_URL=https://m3g6am67aj.execute-api.us-east-2.amazonaws.com/dev" >> "$GITHUB_ENV"
              echo "⚠️  Fallback to dev API endpoint"
            fi
          fi
          
          printf '%s\n' "VITE_AWS_REGION=us-east-2" >> "$GITHUB_ENV"
          sed -i 's/\r$//' "$GITHUB_ENV"

          echo "Building Finanzas SDT Portal (base: /finanzas/)..."
          echo "  Environment: ${DEPLOYMENT_ENV}"
          echo "  API Base URL: ${VITE_API_BASE_URL}"
          BUILD_TARGET=finanzas npm run build || { echo "❌ Finanzas build failed"; exit 1; }
          echo "✅ Finanzas Portal built → dist-finanzas/"

      - name: Configure AWS (OIDC)
        uses: ./.github/actions/oidc-configure-aws
        with:
          role_arn: ${{ secrets.OIDC_AWS_ROLE_ARN }}
          aws_region: ${{ env.AWS_REGION }}
          session_name: deploy-ui-${{ github.run_id }}

      - name: Verify identity
        run: aws sts get-caller-identity

      - name: "Guard - S3 bucket and CloudFront exist"
        run: |
          set -euo pipefail
          echo "Verifying S3 bucket exists..."
          aws s3api head-bucket --bucket "${S3_BUCKET_NAME}" || {
            echo "❌ S3 bucket ${S3_BUCKET_NAME} not found"; exit 1; }
          echo "✅ S3 bucket found: ${S3_BUCKET_NAME}"

          echo "Verifying CloudFront distribution exists..."
          aws cloudfront get-distribution --id "${CLOUDFRONT_DIST_ID}" >/dev/null || {
            echo "❌ CloudFront distribution ${CLOUDFRONT_DIST_ID} not found"; exit 1; }
          echo "✅ CloudFront distribution found: ${CLOUDFRONT_DIST_ID}"

      - name: "Guard - API endpoint present (if provided)"
        if: env.DEV_API_URL != ''
        run: |
          set -euo pipefail
          BASE="${DEV_API_URL%/}"
          echo "Verifying API endpoint exists at $BASE"

          HOST_ID=$(echo "$BASE" | awk -F'[/.]' '{print $3}')
          echo "Extracted API host ID: $HOST_ID"

          if [ "$HOST_ID" != "$EXPECTED_API_ID" ]; then
            echo "❌ Expected API id ${EXPECTED_API_ID}, got ${HOST_ID}"; exit 1;
          fi
          echo "✅ API endpoint ID verified: ${HOST_ID}"

      - name: "Guard - Build artifact validation"
        run: |
          set -euo pipefail
          echo "🔍 Critical Guard: Validating Finanzas build artifacts..."

          # Guard 1: Check that dist-finanzas/index.html uses /finanzas/assets, not /assets
          if grep -R -nE 'src="/assets/|href="/assets/' dist-finanzas/index.html; then
            echo "❌ FAILED: dist-finanzas/index.html points to /assets/* instead of /finanzas/assets/*"
            echo "   This happens when Vite base is not set to /finanzas/"
            echo "   Ensure: vite.config.ts has base: '/finanzas/' for finanzas build"
            exit 1
          fi
          echo "✅ PASS: Asset paths use /finanzas/assets/* correctly"

          # Guard 2: Check for github.dev or codespaces references
          if grep -R -i 'github\.dev\|codespaces' dist-finanzas/ 2>/dev/null; then
            echo "❌ FAILED: Build contains github.dev or codespaces references"
            echo "   Never use Codespaces URLs in production builds"
            exit 1
          fi
          echo "✅ PASS: No github.dev/codespaces references found"

          # Guard 3: Check that index.html doesn't hardcode github domains
          if grep -i 'githubusercontent\|github\.com' dist-finanzas/index.html 2>/dev/null; then
            echo "❌ FAILED: index.html contains hardcoded GitHub domains"
            exit 1
          fi
          echo "✅ PASS: No hardcoded GitHub domains found"

      - name: "Upload PMO Portal to S3 (root /)"
        run: |
          echo "Uploading PMO Portal to S3 root..."
          aws s3 sync dist-pmo/ "s3://${S3_BUCKET_NAME}/" \
            --exclude '*.map' \
            --delete
          echo "✅ PMO Portal uploaded to s3://${S3_BUCKET_NAME}/"

      - name: "Upload Finanzas Portal to S3 (/finanzas prefix)"
        run: |
          echo "Uploading Finanzas Portal to S3 /finanzas/ prefix..."
          aws s3 sync dist-finanzas/ "s3://${S3_BUCKET_NAME}/finanzas/" \
            --exclude '*.map' \
            --delete
          echo "✅ Finanzas Portal uploaded to s3://${S3_BUCKET_NAME}/finanzas/"

      - name: "Guard - Verify CloudFront /finanzas/* Behavior (CRITICAL)"
        run: |
          set -euo pipefail
          echo "🔍 Critical Guard: Verifying CloudFront /finanzas/* behavior exists..."

          # Query specifically for /finanzas/* behavior
          BEHAVIOR_COUNT=$(aws cloudfront get-distribution-config \
            --id "${CLOUDFRONT_DIST_ID}" \
            --query 'DistributionConfig.CacheBehaviors.Items[?PathPattern==`/finanzas/*`] | length(@)' \
            --output text)

          if [ "$BEHAVIOR_COUNT" != "1" ]; then
            echo "❌ CRITICAL: /finanzas/* behavior is MISSING from CloudFront distribution"
            echo "   Finanzas Portal cannot be served correctly without this behavior"
            echo ""
            echo "   To fix this:"
            echo "   1. Go to AWS CloudFront console → Distribution ${CLOUDFRONT_DIST_ID}"
            echo "   2. Select Behaviors tab"
            echo "   3. Create a new behavior with:"
            echo "      - Path pattern: /finanzas/*"
            echo "      - Origin: finanzas-ui-s3 (S3 bucket: ukusi-ui-finanzas-prod)"
            echo "      - Origin access control: finanzas-ui-oac"
            echo "      - Disable SmoothStreaming"
            echo ""
            exit 1
          fi

          echo "✅ PASS: /finanzas/* behavior is configured"

          # Additional verification - check SmoothStreaming is disabled
          SMOOTH_STREAMING=$(aws cloudfront get-distribution-config \
            --id "${CLOUDFRONT_DIST_ID}" \
            --query 'DistributionConfig.CacheBehaviors.Items[?PathPattern==`/finanzas/*`][0].SmoothStreaming' \
            --output text)

          if [ "$SMOOTH_STREAMING" != "false" ]; then
            echo "⚠️  WARNING: /finanzas/* SmoothStreaming is $SMOOTH_STREAMING, expected false"
          else
            echo "   ✓ SmoothStreaming: disabled ✅"
          fi

          # Check target origin
          ORIGIN=$(aws cloudfront get-distribution-config \
            --id "${CLOUDFRONT_DIST_ID}" \
            --query 'DistributionConfig.CacheBehaviors.Items[?PathPattern==`/finanzas/*`][0].TargetOriginId' \
            --output text)

          echo "   ✓ Target origin: $ORIGIN"

          if [ "$ORIGIN" != "finanzas-ui-s3" ]; then
            echo "⚠️  WARNING: /finanzas/* points to '$ORIGIN', expected 'finanzas-ui-s3'"
          else
            echo "   ✓ Origin verification: PASS ✅"
          fi

      - name: "Invalidate CloudFront (all paths + Finanzas SPA)"
        run: |
          set -euo pipefail
          echo "Invalidating CloudFront cache..."
          echo "  Paths: /* (all content), /finanzas/* (SPA paths), /finanzas/index.html (SPA fallback)"

          INVALIDATION_ID=$(aws cloudfront create-invalidation \
            --distribution-id "${CLOUDFRONT_DIST_ID}" \
            --paths '/*' '/finanzas/*' '/finanzas/index.html' \
            --query 'Invalidation.Id' \
            --output text)

          echo "✅ Invalidation queued: $INVALIDATION_ID"
          echo "   Note: Cache propagation may take 30-60 seconds"

      - name: "UI Smoke (PMO root /)"
        env:
          PMO_PREFIX: ""
        run: |
          set -euo pipefail
          DOMAIN=$(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text)
          ROOT="https://${DOMAIN}/"
          STATUS=$(curl -sS -o /dev/null -w '%{http_code}' "$ROOT")
          echo "## PMO Portal (root /)" >> $GITHUB_STEP_SUMMARY
          echo "URL: $ROOT" >> $GITHUB_STEP_SUMMARY
          echo "Status: $STATUS" >> $GITHUB_STEP_SUMMARY

      - name: "UI Smoke (Finanzas /finanzas/)"
        env:
          FINZ_PREFIX: /finanzas
        run: |
          set -euo pipefail
          DOMAIN=$(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text)
          ROOT="https://${DOMAIN}${FINZ_PREFIX}/"
          STATUS=$(curl -sS -o /dev/null -w '%{http_code}' "$ROOT")

          echo "## Finanzas Portal (/finanzas/)" >> $GITHUB_STEP_SUMMARY
          echo "URL: $ROOT" >> $GITHUB_STEP_SUMMARY
          echo "HTTP Status: $STATUS" >> $GITHUB_STEP_SUMMARY

          if [ "$STATUS" = "200" ]; then
            echo "✅ HTTP $STATUS - Portal is accessible" >> $GITHUB_STEP_SUMMARY
            
            # Fetch and verify HTML contains asset references with /finanzas/ prefix
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Asset Path Verification" >> $GITHUB_STEP_SUMMARY
            HTML_SNIPPET=$(curl -sS "$ROOT" | head -n 50)
            
            if echo "$HTML_SNIPPET" | grep -q "/finanzas/assets/"; then
              echo "✅ HTML correctly references /finanzas/assets/*" >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️  HTML may not reference /finanzas/assets correctly" >> $GITHUB_STEP_SUMMARY
            fi
            
            if echo "$HTML_SNIPPET" | grep -q 'github\.dev\|codespaces'; then
              echo "❌ HTML contains github.dev/codespaces references" >> $GITHUB_STEP_SUMMARY
            else
              echo "✅ No github.dev/codespaces references in HTML" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "❌ HTTP $STATUS - Portal is not accessible" >> $GITHUB_STEP_SUMMARY
          fi

      - name: "API Readiness Check (if DEV_API_URL set)"
        if: env.DEV_API_URL != ''
        run: |
          set -euo pipefail
          BASE="${DEV_API_URL%/}"
          echo "Checking API readiness at $BASE"
          for i in $(seq 1 6); do
            CODE=$(curl -sS -o /dev/null -w '%{http_code}' "$BASE/health" || true)
            if [ "$CODE" = "200" ]; then
              echo "API ready (health=200)"
              break
            fi
            echo "Waiting for API... (try $i, code=$CODE)"
            sleep 5
            if [ "$i" = "6" ]; then echo "⚠️  API not ready (will continue)"; fi
          done

      - name: "API Smokes (if DEV_API_URL set)"
        if: env.DEV_API_URL != ''
        env:
          DEV_API_URL: ${{ env.DEV_API_URL }}
        run: |
          set -euo pipefail
          BASE="${DEV_API_URL%/}"
          echo "## Post-Deploy API Smokes" >> $GITHUB_STEP_SUMMARY
          echo "Base: $BASE" >> $GITHUB_STEP_SUMMARY
          echo "Region: us-east-2" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Health (public, no auth required)
          echo "### GET /health (Public)" >> $GITHUB_STEP_SUMMARY
          HEALTH=$(curl -sS -o /dev/null -w '%{http_code}' "$BASE/health" || echo "000")
          if [ "$HEALTH" = "200" ]; then
            curl -sS "$BASE/health" | jq . >> $GITHUB_STEP_SUMMARY || true
            echo "✅ Status: $HEALTH" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️  Status: $HEALTH (may require connectivity setup)" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### Protected Endpoints (catalog/rubros, allocation-rules)" >> $GITHUB_STEP_SUMMARY
          echo "Note: These require Cognito IdToken. Run locally with:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "ID_TOKEN=\$(aws cognito-idp initiate-auth --region us-east-2 --auth-flow USER_PASSWORD_AUTH --client-id dshos5iou44tuach7ta3ici5m --auth-parameters USERNAME=\$USER PASSWORD=\$PASS --query 'AuthenticationResult.IdToken' --output text)" >> $GITHUB_STEP_SUMMARY
          echo "curl -H \"Authorization: Bearer \$ID_TOKEN\" $BASE/catalog/rubros | jq '.data | length'" >> $GITHUB_STEP_SUMMARY
          echo "curl -H \"Authorization: Bearer \$ID_TOKEN\" $BASE/allocation-rules | jq '.data | length'" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: "Newman API Smoke Tests (if DEV_API_URL set)"
        if: env.DEV_API_URL != ''
        continue-on-error: true
        run: |
          set -euo pipefail
          BASE="${DEV_API_URL%/}"
          
          # Install newman if not present
          if ! command -v newman &> /dev/null; then
            npm install -g newman
          fi
          
          # Run Newman tests
          echo "## 🧪 Newman API Smoke Tests" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if newman run postman/finanzas-smokes.json \
            --env-var "API_BASE_URL=${BASE}" \
            --reporters cli,json \
            --reporter-json-export /tmp/newman-report.json 2>&1 | tee /tmp/newman.log; then
            
            echo "✅ **Newman tests passed**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Extract summary from JSON report
            if [ -f /tmp/newman-report.json ]; then
              echo "### Test Summary" >> $GITHUB_STEP_SUMMARY
              jq -r '.run.stats | "- **Total Requests**: \(.requests.total)\n- **Assertions**: \(.assertions.total)\n- **Passed**: \(.assertions.total - .assertions.failed)\n- **Failed**: \(.assertions.failed)"' /tmp/newman-report.json >> $GITHUB_STEP_SUMMARY || true
            fi
          else
            echo "⚠️  **Newman tests had failures** (non-blocking)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            tail -20 /tmp/newman.log >> $GITHUB_STEP_SUMMARY || true
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: "Summary & Evidence Pack"
        run: |
          set -euo pipefail
          {
            echo "# 🚀 Finanzas UI Deployment Complete"
            echo ""
            echo "## 📊 Deployment Information"
            echo ""
            echo "| Property | Value |"
            echo "|----------|-------|"
            echo "| **Environment** | **${DEPLOYMENT_ENV}** |"
            echo "| Build Time | $(date -u +'%Y-%m-%d %H:%M:%S UTC') |"
            echo "| Commit SHA | \`$(git rev-parse --short HEAD)\` |"
            echo "| Commit Message | $(git log -1 --pretty=%B | head -1) |"
            echo "| Branch | ${GITHUB_REF#refs/heads/} |"
            echo "| Workflow Run | [#${GITHUB_RUN_NUMBER}](${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}) |"
            echo ""
            echo "## 🎯 Build Targets"
            echo ""
            echo "- **PMO Portal**: Root path (/) → dist-pmo/"
            echo "- **Finanzas Portal**: /finanzas/ prefix → dist-finanzas/"
            echo ""
            echo "## ⚙️  Environment Configuration"
            echo ""
            echo "| Variable | Value |"
            echo "|----------|-------|"
            echo "| DEPLOYMENT_ENV | ${DEPLOYMENT_ENV} |"
            echo "| VITE_PUBLIC_BASE | /finanzas/ |"
            echo "| VITE_FINZ_ENABLED | true |"
            echo "| VITE_USE_MOCKS | false |"
            echo "| VITE_API_BASE_URL | ${VITE_API_BASE_URL:-'(not set)'} |"
            echo "| VITE_AWS_REGION | us-east-2 |"
            echo "| BUILD_TARGET | finanzas |"
            echo "| EXPECTED_API_ID | ${EXPECTED_API_ID} |"
            echo ""
            echo "## 📦 AWS Infrastructure"
            echo ""
            echo "| Resource | Value |"
            echo "|----------|-------|"
            echo "| S3 Bucket | \`${S3_BUCKET_NAME}\` |"
            echo "| S3 Prefix (PMO) | \`s3://${S3_BUCKET_NAME}/\` |"
            echo "| S3 Prefix (Finanzas) | \`s3://${S3_BUCKET_NAME}/finanzas/\` |"
            echo "| CloudFront Distribution | \`${CLOUDFRONT_DIST_ID}\` |"
            echo "| CloudFront Domain | $(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text) |"
            echo "| API Gateway ID | \`${EXPECTED_API_ID}\` |"
            echo "| API Endpoint | ${VITE_API_BASE_URL:-'(not set)'} |"
            echo ""
            echo "## ✅ Deployment Checklist"
            echo ""
            echo "### Build Configuration"
            echo "- [x] Vite base = /finanzas/ (BUILD_TARGET=finanzas)"
            echo "- [x] Router basename = /finanzas"
            echo "- [x] API client uses VITE_API_BASE_URL (not window.location.origin)"
            echo "- [x] No github.dev/codespaces references"
            echo "- [x] dist-finanzas/index.html uses /finanzas/assets/*"
            echo ""
            echo "### S3 Upload"
            echo "- [x] PMO Portal uploaded to s3://${S3_BUCKET_NAME}/"
            echo "- [x] Finanzas Portal uploaded to s3://${S3_BUCKET_NAME}/finanzas/"
            echo "- [x] Source maps excluded from upload"
            echo "- [x] Old files cleaned up (--delete flag)"
            echo ""
            echo "### CloudFront"
            echo "- [x] Distribution: ${CLOUDFRONT_DIST_ID}"
            echo "- [x] /finanzas/* behavior exists → finanzas-ui-s3 origin"
            echo "- [x] SmoothStreaming: disabled"
            echo "- [x] Cache invalidation: /*, /finanzas/*, /finanzas/index.html"
            echo ""
            echo "### Cognito Configuration"
            echo "- **User Pool ID**: us-east-2_FyHLtOhiY"
            echo "- **App Client ID**: dshos5iou44tuach7ta3ici5m"
            echo "- **Callback URL**: https://d7t9x3j66yd8k.cloudfront.net/finanzas/"
            echo "- **Sign-out URL**: https://d7t9x3j66yd8k.cloudfront.net/finanzas/"
            echo "- **Region**: us-east-2"
            echo ""
            DIST_DOMAIN=$(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text)
            echo "## 🌐 Access Points"
            echo ""
            echo "### User Interface"
            echo "| Portal | URL |"
            echo "|--------|-----|"
            echo "| PMO Portal | https://${DIST_DOMAIN}/ |"
            echo "| **Finanzas Portal** | **https://${DIST_DOMAIN}/finanzas/** |"
            echo "| Diagnostics | https://${DIST_DOMAIN}/finanzas/_diag |"
            echo ""
            echo "### API Endpoints"
            echo "| Endpoint | URL | Auth |"
            echo "|----------|-----|------|"
            echo "| Health Check | ${VITE_API_BASE_URL}/health | Public |"
            echo "| Catalog (Rubros) | ${VITE_API_BASE_URL}/catalog/rubros | Cognito JWT |"
            echo "| Allocation Rules | ${VITE_API_BASE_URL}/allocation-rules | Cognito JWT |"
            echo ""
            echo "## 🔍 Verification Commands"
            echo ""
            echo "### 1. Verify CloudFront Configuration"
            echo "\`\`\`bash"
            echo "# Check /finanzas/* behavior"
            echo "aws cloudfront get-distribution --id ${CLOUDFRONT_DIST_ID} \\"
            echo "  --query \"DistributionConfig.CacheBehaviors.Items[?PathPattern=='/finanzas/*']\" \\"
            echo "  --output json | jq '.[0] | {TargetOriginId, SmoothStreaming}'"
            echo "# Expected: {\"TargetOriginId\": \"finanzas-ui-s3\", \"SmoothStreaming\": false}"
            echo "\`\`\`"
            echo ""
            echo "### 2. Verify UI Deployment"
            echo "\`\`\`bash"
            echo "# Test Finanzas portal is accessible"
            echo "curl -I https://${DIST_DOMAIN}/finanzas/"
            echo ""
            echo "# Verify asset paths use /finanzas/ prefix"
            echo "curl -s https://${DIST_DOMAIN}/finanzas/ | grep -o '/finanzas/assets/[^\"]*' | head -3"
            echo ""
            echo "# Ensure no github.dev references"
            echo "curl -s https://${DIST_DOMAIN}/finanzas/ | grep -i 'github.dev\\|codespaces' || echo '✅ No dev references'"
            echo "\`\`\`"
            echo ""
            echo "### 3. Test API Connectivity (with Cognito)"
            echo "\`\`\`bash"
            echo "# Get Cognito ID token (replace USERNAME and PASSWORD)"
            echo "ID_TOKEN=\\\$(aws cognito-idp initiate-auth \\"
            echo "  --region us-east-2 \\"
            echo "  --auth-flow USER_PASSWORD_AUTH \\"
            echo "  --client-id dshos5iou44tuach7ta3ici5m \\"
            echo "  --auth-parameters USERNAME=YOUR_USERNAME PASSWORD=YOUR_PASSWORD \\"
            echo "  --query 'AuthenticationResult.IdToken' --output text)"
            echo ""
            echo "# Test public endpoint"
            echo "curl -fsS ${VITE_API_BASE_URL}/health | jq '.'"
            echo ""
            echo "# Test protected endpoints"
            echo "curl -fsS -H \"Authorization: Bearer \\\$ID_TOKEN\" \\"
            echo "  ${VITE_API_BASE_URL}/catalog/rubros | jq '.data | length'"
            echo ""
            echo "curl -fsS -H \"Authorization: Bearer \\\$ID_TOKEN\" \\"
            echo "  ${VITE_API_BASE_URL}/allocation-rules | jq '.data | length'"
            echo "\`\`\`"
            echo ""
            echo "### 4. End-to-End Test"
            echo ""
            echo "1. **Navigate to Finanzas Portal**: https://${DIST_DOMAIN}/finanzas/"
            echo "2. **Login with Cognito**: Use test credentials"
            echo "3. **Verify JWT Token**: Check browser DevTools Network tab for Authorization header"
            echo "4. **Test Navigation**: Navigate to /finanzas/catalog/rubros"
            echo "5. **Verify Data Load**: Confirm catalog data is displayed"
            echo "6. **Check Console**: No errors in browser console"
            echo ""
            echo "## ⏱️  Cache Information"
            echo ""
            echo "| Resource Type | Cache-Control | CloudFront TTL |"
            echo "|--------------|---------------|----------------|"
            echo "| index.html | no-store | 0s (always revalidate) |"
            echo "| Assets (/finanzas/assets/*) | public, max-age=31536000 | 1 year (immutable) |"
            echo "| API Responses | Depends on API headers | Varies by endpoint |"
            echo ""
            echo "**Note**: Cache invalidation has been triggered for:"
            echo "- \`/*\` (all content)"
            echo "- \`/finanzas/*\` (SPA paths)"
            echo "- \`/finanzas/index.html\` (SPA fallback)"
            echo ""
            echo "Cache propagation typically takes 30-60 seconds."
            echo ""
            echo "## 📝 Deployment Summary"
            echo ""
            echo "This deployment includes:"
            echo "- ✅ PMO Portal at root path"
            echo "- ✅ Finanzas Portal at /finanzas/ with correct base path"
            echo "- ✅ Environment-specific API endpoint (${DEPLOYMENT_ENV}: ${VITE_API_BASE_URL})"
            echo "- ✅ CloudFront cache invalidation"
            echo "- ✅ Proper Cognito authentication configuration"
            echo "- ✅ Build artifact validation (no github.dev references)"
            echo ""
            echo "**Deployment Status**: ✅ Complete"
          } >> $GITHUB_STEP_SUMMARY

      - name: Summary (Legacy)
        run: |
          {
            echo "## UIs Deployed (dev)"
            echo ""
            echo "### PMO Portal"
            echo "- Path: / (CloudFront root)"
            echo "- Build: dist-pmo/"
            echo ""
            echo "### Finanzas SDT Portal"
            echo "- Path: /finanzas/"
            echo "- Build: dist-finanzas/"
            echo "- API Base: ${VITE_API_BASE_URL:-'(not set)'}"
            echo "- API: ${FINZ_API_STACK}"
            echo ""
            echo "### Access"
            echo "- PMO: https://d7t9x3j66yd8k.cloudfront.net/"
            echo "- Finanzas: https://d7t9x3j66yd8k.cloudfront.net/finanzas/"
          } >> $GITHUB_STEP_SUMMARY

name: Deploy Finanzas UI

on:
  workflow_dispatch: {}
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

# --------- GLOBAL ENV (branch-aware) ----------
env:
  AWS_REGION: ${{ vars.AWS_REGION || 'us-east-2' }}

  # Branch → environment (UI only, API stays on dev for now)
  DEPLOYMENT_ENV: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}

  # S3 bucket for Finanzas (PMO is deployed by acta-ui repo)
  FINANZAS_BUCKET_NAME: ${{ vars.FINANZAS_BUCKET_NAME || 'ukusi-ui-finanzas-prod' }}

  # CloudFront (single distribution)
  CLOUDFRONT_DIST_ID: ${{ vars.CLOUDFRONT_DIST_ID || 'EPQU7PVDLQXUA' }}
  CF_DOMAIN: ${{ vars.CF_DOMAIN || 'd7t9x3j66yd8k.cloudfront.net' }}

  # API ID / Stage (ID from vars, stage by branch)
  FINZ_API_ID: ${{ vars.FINZ_API_ID || 'pyorjw6lbe' }}
  FINZ_API_STAGE: ${{ github.ref == 'refs/heads/main' && (vars.FINZ_API_STAGE_PROD || 'prod') || (vars.FINZ_API_STAGE_DEV || 'dev') }}
  EXPECTED_API_ID: ${{ vars.FINZ_API_ID || 'pyorjw6lbe' }}

  # Optional escape hatch: explicit API base (dev-only override, but OK to use as canonical)
  DEV_API_URL: ${{ vars.DEV_API_URL || '' }}

  # Cognito Configuration (required for authentication)
  COGNITO_USER_POOL_ID: ${{ vars.COGNITO_USER_POOL_ID }}
  COGNITO_WEB_CLIENT: ${{ vars.COGNITO_WEB_CLIENT }}
  COGNITO_DOMAIN: ${{ vars.COGNITO_DOMAIN }}
  COGNITO_IDENTITY_POOL_ID: ${{ vars.COGNITO_IDENTITY_POOL_ID || '' }}

  # Frontend feature flags
  VITE_FINZ_ENABLED: ${{ vars.VITE_FINZ_ENABLED || 'true' }}
  VITE_USE_MOCKS: "false"

jobs:
  build-and-deploy-all:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ---- Preflight env (same strategy as before) ----
      - name: Preflight env
        run: |
          set -euo pipefail
          echo "🚀 Deployment Environment: ${DEPLOYMENT_ENV}"
          for k in AWS_REGION FINANZAS_BUCKET_NAME CLOUDFRONT_DIST_ID FINZ_API_ID FINZ_API_STAGE COGNITO_USER_POOL_ID COGNITO_WEB_CLIENT COGNITO_DOMAIN; do
            v="${!k:-}"; [ -n "$v" ] || { echo "❌ Missing repo var: $k"; exit 1; }
          done
          printf '%s\n' "VITE_USE_MOCKS=false" >> "$GITHUB_ENV"
          sed -i 's/\r$//' "$GITHUB_ENV"
          echo "✅ Env OK"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install CLI tools
        run: |
          set -euo pipefail
          echo "Installing required CLI tools for validation..."
          sudo apt-get update -qq
          sudo apt-get install -y -qq curl jq dnsutils
          echo "✅ CLI tools installed"

      - name: Install deps
        run: |
          set -e
          echo "Installing frontend dependencies (prefer lock; fallback to install)"
          if ! npm ci; then
            echo "⚠️ npm ci failed; running npm install"
            npm install
          fi

      # ---- Lint (auto-fix where possible, then gate) ----
      - name: Lint (auto-fix if supported)
        run: |
          set -euo pipefail
          # Detect lint script
          LINT_SCRIPT=$(node -p "require('./package.json').scripts && require('./package.json').scripts.lint || ''" || echo "")
          if [ -z "$LINT_SCRIPT" ]; then
            echo "ℹ️  No lint script defined; skipping lint step"
            exit 0
          fi

          echo "ℹ️  Running npm run lint -- --fix (best-effort auto-correct)"
          if ! npm run lint -- --fix; then
            echo "⚠️  npm run lint -- --fix failed or not supported; will still run plain lint"
          fi

          echo "ℹ️  Running npm run lint (gating)"
          npm run lint

      # ---- Compute the API base URL for the build (DEV_API_URL wins, otherwise derive) ----
      - name: Compute API base URL
        id: apibase
        run: |
          set -euo pipefail

          if [ -n "${DEV_API_URL}" ]; then
            BASE="${DEV_API_URL%/}"
            echo "✅ Using DEV_API_URL override: $BASE"
          else
            BASE="https://${FINZ_API_ID}.execute-api.${AWS_REGION}.amazonaws.com/${FINZ_API_STAGE}"
            echo "✅ Using constructed API endpoint: $BASE"
          fi

          if [ -z "$BASE" ]; then
            echo "❌ CRITICAL: API base URL is empty. Set DEV_API_URL repo variable or ensure FINZ_API_ID/AWS_REGION/FINZ_API_STAGE are set."
            exit 1
          fi

          HOST_WITH_PATH="${BASE#https://}"
          HOST="${HOST_WITH_PATH%%/*}"
          EXPECTED_HOST="${EXPECTED_API_ID}.execute-api.${AWS_REGION}.amazonaws.com"
          if [[ -n "${EXPECTED_API_ID}" && "$HOST" != "$EXPECTED_HOST" ]]; then
            echo "⚠️  WARNING: API host ($HOST) does not match EXPECTED_API_ID ($EXPECTED_HOST)" >&2
          fi

          echo "📝 Setting VITE_API_BASE_URL=$BASE for Finanzas build"
          printf '%s\n' "VITE_API_BASE_URL=$BASE" >> "$GITHUB_ENV"
          sed -i 's/\r$//' "$GITHUB_ENV"

      - name: Guard - Stage/Environment Drift Check
        run: |
          set -euo pipefail

          ALLOW_DRIFT="${{ vars.ALLOW_STAGE_DRIFT || 'false' }}"

          echo "🔍 Checking for stage/environment drift..."
          echo "   DEPLOYMENT_ENV: ${DEPLOYMENT_ENV}"
          echo "   FINZ_API_STAGE: ${FINZ_API_STAGE}"
          echo "   ALLOW_STAGE_DRIFT: ${ALLOW_DRIFT}"

          if [ "${DEPLOYMENT_ENV}" = "prod" ] && [ "${FINZ_API_STAGE}" != "prod" ]; then
            if [ "${ALLOW_DRIFT}" != "true" ]; then
              echo "❌ CRITICAL: Production deployment with non-production API stage"
              echo "   DEPLOYMENT_ENV is 'prod' but FINZ_API_STAGE is '${FINZ_API_STAGE}'"
              echo "   This would deploy production frontend pointing to ${FINZ_API_STAGE} API"
              echo ""
              echo "🔧 To fix:"
              echo "   1. Set FINZ_API_STAGE_PROD=prod in repository variables"
              echo "   2. Or set ALLOW_STAGE_DRIFT=true to override this check"
              exit 1
            else
              echo "⚠️  WARNING: Stage drift allowed (ALLOW_STAGE_DRIFT=true)"
              echo "   Production deployment will use ${FINZ_API_STAGE} API"
            fi
          else
            echo "✅ Stage/environment alignment verified"
          fi

      - name: Preflight API health
        run: |
          set -euo pipefail
          : ${VITE_API_BASE_URL:?VITE_API_BASE_URL missing after compute step}
          BASE="${VITE_API_BASE_URL%/}"
          echo "Checking ${BASE}/health before build..."
          STATUS=$(curl -sS -o /dev/null -w '%{http_code}' "${BASE}/health" || true)
          if [ "$STATUS" != "200" ]; then
            echo "❌ API /health preflight failed with HTTP $STATUS" >&2
            exit 1
          fi
          echo "✅ API /health preflight ok"

      - name: Build Finanzas SDT Portal (base /finanzas/)
        env:
          BUILD_TARGET: finanzas
          VITE_API_BASE_URL: ${{ env.VITE_API_BASE_URL }}
          VITE_ACTA_BASE_URL: ${{ vars.VITE_ACTA_BASE_URL }}
          VITE_PREFACTURAS_URL: ${{ vars.VITE_PREFACTURAS_URL }}
          VITE_BASE_URL: "/finanzas/"
          VITE_FINZ_ENABLED: "true"
          VITE_USE_MOCKS: "false"
          VITE_PUBLIC_BASE: "/finanzas/"
          VITE_AWS_REGION: ${{ env.AWS_REGION }}
          VITE_COGNITO_REGION: ${{ env.AWS_REGION }}
          VITE_COGNITO_USER_POOL_ID: ${{ env.COGNITO_USER_POOL_ID }}
          VITE_COGNITO_CLIENT_ID: ${{ env.COGNITO_WEB_CLIENT }}
          VITE_COGNITO_IDENTITY_POOL_ID: ${{ env.COGNITO_IDENTITY_POOL_ID }}
          VITE_COGNITO_DOMAIN: ${{ env.COGNITO_DOMAIN }}
          VITE_CLOUDFRONT_URL: https://${{ env.CF_DOMAIN }}
        run: |
          set -euo pipefail

          VITE_API_BASE_URL="$(printf '%s' "$VITE_API_BASE_URL" | tr -d '\r\n')"
          VITE_API_BASE_URL="${VITE_API_BASE_URL%/}"

          if [ -z "$VITE_API_BASE_URL" ]; then
            echo "❌ CRITICAL: VITE_API_BASE_URL is empty"
            echo "   This variable is REQUIRED for Finanzas builds"
            echo "   Set DEV_API_URL in repository variables"
            exit 1
          fi

          echo "[Finanzas] BUILD_TARGET=$BUILD_TARGET"
          echo "[Finanzas] VITE_API_BASE_URL=$VITE_API_BASE_URL"
          echo "[Finanzas] Building Finanzas SDT Portal (base: /finanzas/)..."
          echo "[Finanzas] Environment: ${DEPLOYMENT_ENV}"

          export VITE_API_BASE_URL

          BUILD_TARGET=finanzas npm run build || {
            echo "❌ Finanzas build failed"
            echo "   Most likely cause: VITE_API_BASE_URL not properly configured"
            exit 1
          }
          echo "✅ Finanzas Portal built → dist-finanzas/"

      - name: Record Finanzas bundle hashes
        run: |
          set -euo pipefail
          ls -R dist-finanzas
          sha256sum dist-finanzas/assets/index-*.js dist-finanzas/assets/index-*.css > dist-finanzas-bundle-hashes.txt
          {
            echo "### Finanzas bundle hashes"
            echo ""
            cat dist-finanzas-bundle-hashes.txt
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Finanzas bundle hash artifact
        uses: actions/upload-artifact@v4
        with:
          name: finanzas-bundle-hashes
          path: dist-finanzas-bundle-hashes.txt

      - name: Find previous successful Finanzas run
        id: prev_run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          BRANCH_NAME="${GITHUB_REF##*/}"
          echo "🔎 Looking for previous successful run on branch: $BRANCH_NAME"

          # Try to fetch last 20 runs; if this fails (e.g. 403), degrade gracefully
          if ! RAW_JSON=$(gh run list \
              --workflow "Deploy Finanzas UI" \
              --branch "$BRANCH_NAME" \
              --json databaseId,headSha,conclusion,status \
              -L 20 2>/tmp/gh_run_list_err.log); then
            echo "ℹ️  gh run list failed (likely permission issue). Skipping previous-run hash comparison."
            echo "----- gh run list stderr -----"
            cat /tmp/gh_run_list_err.log || true
            printf 'previous_run_found=false\n' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          PREV_RUN_ID=$(printf '%s\n' "$RAW_JSON" \
            | jq -r --arg current "$GITHUB_SHA" '
                [ .[]
                  | select(.conclusion=="success" and .headSha != $current)
                ][0].databaseId // ""
              ')

          if [ -z "$PREV_RUN_ID" ]; then
            echo "ℹ️  No previous successful run found for this branch (or only current SHA present)."
            printf 'previous_run_found=false\n' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "✅ Previous successful run id: $PREV_RUN_ID"

          mkdir -p /tmp/prev-hashes

          if gh run download "$PREV_RUN_ID" --name finanzas-bundle-hashes --dir /tmp/prev-hashes 2>/tmp/gh_run_download_err.log; then
            if [ -f /tmp/prev-hashes/dist-finanzas-bundle-hashes.txt ]; then
              printf 'previous_run_found=true\n' >> "$GITHUB_OUTPUT"
              printf 'previous_hash_file=/tmp/prev-hashes/dist-finanzas-bundle-hashes.txt\n' >> "$GITHUB_OUTPUT"
              echo "✅ Previous hash file found at /tmp/prev-hashes/dist-finanzas-bundle-hashes.txt"
            else
              echo "ℹ️  Previous run had no dist-finanzas-bundle-hashes.txt artifact."
              printf 'previous_run_found=false\n' >> "$GITHUB_OUTPUT"
            fi
          else
            echo "ℹ️  gh run download failed (likely permission issue). Skipping previous-run hash comparison."
            echo "----- gh run download stderr -----"
            cat /tmp/gh_run_download_err.log || true
            printf 'previous_run_found=false\n' >> "$GITHUB_OUTPUT"
          fi

      - name: Compare bundle hashes with previous run
        if: steps.prev_run.outputs.previous_run_found == 'true'
        run: |
          set -euo pipefail
          PREV_FILE="${{ steps.prev_run.outputs.previous_hash_file }}"
          echo "🔍 Comparing current bundle hashes with: $PREV_FILE"
          if diff -u "$PREV_FILE" dist-finanzas-bundle-hashes.txt; then
            echo "ℹ️  Bundle hashes match previous successful run."
          else
            echo "ℹ️  Bundle hashes differ from previous run (expected when Finanzas UI changed)."
          fi

      - name: Detect Finanzas UI changes in this commit
        id: finz_changes
        run: |
          set -euo pipefail

          COMPARE_REF="${{ github.event.before || '' }}"
          if [ -z "$COMPARE_REF" ]; then
            COMPARE_REF="HEAD~1"
          fi

          if ! git rev-parse "$COMPARE_REF" >/dev/null 2>&1; then
            COMPARE_REF="HEAD~1"
          fi

          CHANGED_FILES=$(git diff --name-only "$COMPARE_REF"...HEAD || git diff --name-only "$COMPARE_REF" HEAD || true)
          echo "$CHANGED_FILES" > /tmp/finz-changed-files.txt

          if echo "$CHANGED_FILES" | grep -E -q '^(src/|public/|index\.html|vite\.config\.ts|package\.json|tailwind\.config\.js|tsconfig|scripts/finanzas)'; then
            printf 'finz_changed=true\n' >> "$GITHUB_OUTPUT"
          else
            printf 'finz_changed=false\n' >> "$GITHUB_OUTPUT"
          fi

          {
            echo "### Finanzas file changes (detected vs $COMPARE_REF)"
            echo "$(wc -l < /tmp/finz-changed-files.txt) files changed"
            head -50 /tmp/finz-changed-files.txt
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Fail if bundle unchanged while Finanzas files changed
        if: steps.prev_run.outputs.previous_run_found == 'true'
        run: |
          set -euo pipefail

          if cmp -s dist-finanzas-bundle-hashes.txt "${{ steps.prev_run.outputs.previous_hash_file }}"; then
            if [ "${{ steps.finz_changes.outputs.finz_changed }}" = "true" ]; then
              echo "❌ Finanzas bundle hashes match previous successful deploy, but Finanzas files changed."
              exit 1
            else
              echo "ℹ️ Bundle hashes unchanged and no Finanzas-specific files changed; continuing."
            fi
          else
            echo "✅ Bundle hashes differ from previous successful deploy."
          fi

      - name: Verify API URL embedded in Finanzas bundle
        env:
          VITE_API_BASE_URL: ${{ env.VITE_API_BASE_URL }}
        run: |
          set -euo pipefail

          VITE_API_BASE_URL="$(printf '%s' "$VITE_API_BASE_URL" | tr -d '\r\n')"
          VITE_API_BASE_URL="${VITE_API_BASE_URL%/}"

          DIST="dist-finanzas"
          echo "🔍 Searching for VITE_API_BASE_URL in build artifacts..."

          if ! grep -R --binary-files=text -F "$VITE_API_BASE_URL" "$DIST" >/dev/null; then
            echo "❌ CRITICAL: VITE_API_BASE_URL ($VITE_API_BASE_URL) not found in $DIST"
            echo "   This means the API URL was not injected during build"
            echo ""
            echo "🔧 Troubleshooting:"
            echo "   1. Check that VITE_API_BASE_URL was set during build"
            echo "   2. Verify vite.config.ts is properly injecting the value"
            echo "   3. Review build logs for Vite configuration errors"
            exit 1
          fi

          echo "✅ VITE_API_BASE_URL found in Finanzas bundle"
          echo "📋 Sample occurrences:"
          grep -R --binary-files=text -F "$VITE_API_BASE_URL" "$DIST" | head -3 || true

      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.OIDC_AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: deploy-ui-${{ github.run_id }}

      - name: Verify identity
        run: aws sts get-caller-identity

      - name: Guard - S3 buckets and CloudFront exist
        run: |
          set -euo pipefail
          aws s3api head-bucket --bucket "${FINANZAS_BUCKET_NAME}" >/dev/null || { echo "❌ Missing Finanzas bucket ${FINANZAS_BUCKET_NAME}"; exit 1; }
          aws cloudfront get-distribution --id "${CLOUDFRONT_DIST_ID}" >/dev/null || { echo "❌ Missing CloudFront distribution ${CLOUDFRONT_DIST_ID}"; exit 1; }
          echo "✅ Buckets & CloudFront found"

      - name: Guard - API endpoint present (VITE_API_BASE_URL)
        run: |
          set -euo pipefail
          : ${VITE_API_BASE_URL:?VITE_API_BASE_URL missing}
          BASE="${VITE_API_BASE_URL%/}"
          HOST_ID=$(echo "$BASE" | awk -F'[/.]' '{print $3}')
          : ${EXPECTED_API_ID:?EXPECTED_API_ID missing}
          if [ "$HOST_ID" != "$EXPECTED_API_ID" ]; then
            echo "❌ Expected API id ${EXPECTED_API_ID}, got ${HOST_ID}"; exit 1;
          fi
          CODE=$(curl -sS -o /dev/null -w '%{http_code}' "$BASE/health" || true)
          if [ "$CODE" != "200" ]; then
            echo "❌ /health returned $CODE — check stage deployment"; exit 1;
          fi
          echo "✅ API endpoint ID and /health verified: $BASE"

      - name: Guard - Finanzas build artifacts
        run: |
          set -euo pipefail
          if grep -R -nE 'src="/assets/|href="/assets/' dist-finanzas/index.html; then
            echo "❌ dist-finanzas/index.html points to /assets/* (missing base '/finanzas/')"; exit 1;
          fi
          if [ ! -f dist-finanzas/auth/callback.html ]; then
            echo "❌ Missing auth callback asset at dist-finanzas/auth/callback.html (expected from public/auth/callback.html)"
            exit 1
          fi
          if [ -f dist-finanzas/finanzas/auth/callback.html ]; then
            echo "❌ Unexpected nested callback found at dist-finanzas/finanzas/auth/callback.html"
            echo "   This would publish to /finanzas/finanzas/auth/callback.html and break the Hosted UI redirect"
            exit 1
          fi
          if grep -R -i 'github\.dev\|codespaces' dist-finanzas/ 2>/dev/null; then
            echo "❌ Build contains github.dev/codespaces references"; exit 1;
          fi
          if find dist-finanzas -name "aws-exports.js" -o -name "aws-exports.ts" | grep -q .; then
            echo "❌ CRITICAL: aws-exports.js found in Finanzas build!";
            echo "   aws-exports.js is a PMO/Amplify concern and should NOT be in Finanzas";
            exit 1;
          fi
          echo "✅ Finanzas build artifacts look good"

      - name: Upload Finanzas Portal to S3 (/finanzas prefix)
        run: |
          aws s3 sync dist-finanzas/ "s3://${FINANZAS_BUCKET_NAME}/finanzas/" \
            --exclude '*.map' \
            --exclude 'docs/*' \
            --exclude 'test-helper.html' \
            --delete
          echo "✅ Finanzas uploaded to s3://${FINANZAS_BUCKET_NAME}/finanzas/"

      - name: Verify Finanzas S3 manifest matches local build
        run: |
          set -euo pipefail

          # Compare only the SPA assets we actually sync (exclude source maps, legacy docx, and docs/ bundles managed elsewhere)
          find dist-finanzas -type f \
            ! -name '*.map' \
            ! -name '*.docx' \
            ! -path 'dist-finanzas/docs/*' \
            -print \
            | sed 's|^dist-finanzas/||' \
            | sort > /tmp/local-finanzas-manifest.txt

          aws s3api list-objects-v2 \
            --bucket "${FINANZAS_BUCKET_NAME}" \
            --prefix "finanzas/" \
            --output json \
            | jq -r '.Contents[]?.Key
              | select(. | endswith(".map") | not)
              | select(. | endswith(".docx") | not)
              | select(. | startswith("docs/") | not)
              | sub("^finanzas/"; "")' \
            | sort > /tmp/s3-finanzas-manifest.txt

          echo "🔍 Comparing local vs S3 manifests (excluding .map/.docx files and docs/ prefix)..."
          if ! diff -u /tmp/local-finanzas-manifest.txt /tmp/s3-finanzas-manifest.txt; then
            echo "❌ Finanzas S3 manifest mismatch — bucket drift detected"
            echo "   Ensure sync completed successfully and CloudFront invalidation is executed"
            exit 1
          fi

          echo "✅ Finanzas S3 manifest matches local build"

      - name: Verify auth callback in S3
        run: |
          set -euo pipefail
          if ! aws s3api head-object --bucket "${FINANZAS_BUCKET_NAME}" --key "finanzas/auth/callback.html" >/dev/null 2>&1; then
            echo "❌ auth/callback.html missing in S3 at finanzas/auth/callback.html"
            echo "   Ensure public/auth/callback.html exists and dist-finanzas/auth/callback.html is produced by the build"
            exit 1
          fi
          echo "✅ auth/callback.html present in S3 at finanzas/auth/callback.html"

      - name: Guard - Verify CloudFront /finanzas/* Behavior (CRITICAL)
        run: |
          set -euo pipefail
          BEHAVIOR_COUNT=$(aws cloudfront get-distribution-config \
            --id "${CLOUDFRONT_DIST_ID}" \
            --query 'DistributionConfig.CacheBehaviors.Items[?PathPattern==`/finanzas/*`] | length(@)' \
            --output text)
          if [ "$BEHAVIOR_COUNT" != "1" ]; then
            echo "❌ CRITICAL: /finanzas/* behavior missing in CloudFront ${CLOUDFRONT_DIST_ID}"; exit 1;
          fi
          echo "✅ /finanzas/* behavior configured"

      - name: Invalidate CloudFront (all + Finanzas SPA)
        run: |
          set -euo pipefail
          INVALIDATION_ID=$(aws cloudfront create-invalidation \
            --distribution-id "${CLOUDFRONT_DIST_ID}" \
            --paths '/*' '/finanzas/*' '/finanzas/index.html' \
            --query 'Invalidation.Id' --output text)
          echo "✅ Invalidation queued: $INVALIDATION_ID"

      - name: UI Smoke (Finanzas /finanzas/)
        run: |
          set -euo pipefail
          DOMAIN="${CF_DOMAIN}"
          if [ -z "$DOMAIN" ]; then
            DOMAIN=$(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text)
          fi
          FINZ="https://${DOMAIN}/finanzas/"
          STATUS=$(curl -sS -o /dev/null -w '%{http_code}' "$FINZ")
          echo "Finanzas /finanzas/ status: $STATUS"
          if [ "$STATUS" = "200" ]; then
            HTML_SNIPPET=$(curl -sS "$FINZ" | head -n 40)
            echo "$HTML_SNIPPET" | grep -q "/finanzas/assets/" || { echo "⚠️ HTML missing /finanzas/assets references"; }
          else
            echo "❌ Finanzas portal not accessible (HTTP $STATUS)"; exit 1;
          fi

      - name: Deep-route SPA smoke test
        run: |
          set -euo pipefail
          DOMAIN="${CF_DOMAIN}"
          if [ -z "$DOMAIN" ]; then
            DOMAIN=$(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text)
          fi

          DEEP_ROUTE="https://${DOMAIN}/finanzas/sdmt/cost/forecast"
          echo "Testing deep SPA route: $DEEP_ROUTE"

          STATUS=$(curl -sS -o /tmp/deep_route.html -w '%{http_code}' "$DEEP_ROUTE" 2>/dev/null || echo "000")
          echo "Deep route status: $STATUS"

          if [ "$STATUS" != "200" ]; then
            echo "❌ Deep-route test failed (HTTP $STATUS)"
            echo "   SPA routing may not be configured correctly"
            exit 1
          fi

          if [ -f /tmp/deep_route.html ]; then
            if grep -q "/finanzas/assets/" /tmp/deep_route.html; then
              echo "✅ Deep-route returns valid SPA content (found /finanzas/assets/)"
            else
              echo "❌ Deep-route response missing /finanzas/assets/ references"
              echo "   This may indicate CloudFront is not handling SPA routing correctly"
              exit 1
            fi
          fi

          echo "✅ Deep-route SPA smoke test passed"

      - name: API Smokes (public /health, /catalog/rubros)
        run: |
          set -euo pipefail
          : ${VITE_API_BASE_URL:?VITE_API_BASE_URL missing}
          BASE="${VITE_API_BASE_URL%/}"
          echo "API base: $BASE"
          CODE=$(curl -sS -o /dev/null -w '%{http_code}' "$BASE/health" || true)
          [ "$CODE" = "200" ] || { echo "❌ /health returned $CODE"; exit 1; }
          LEN=$(curl -sS "$BASE/catalog/rubros" | jq '.data | length' || echo "0")
          echo "Catalog items: $LEN"
          [ "$LEN" != "0" ] || echo "⚠️ catalog/rubros length is 0 (seed missing?)"

      - name: Post-Deployment Comprehensive Verification
        run: |
          set -euo pipefail
          export CLOUDFRONT_DOMAIN="${CF_DOMAIN}"
          export VITE_API_BASE_URL="${VITE_API_BASE_URL}"
          export S3_BUCKET="${FINANZAS_BUCKET_NAME}"

          echo "🔍 Running comprehensive post-deployment verification..."
          bash scripts/post-deploy-verify.sh || {
            echo "❌ Post-deployment verification failed"
            echo "   This indicates the deployment may not be functioning correctly"
            echo "   Review the errors above before proceeding"
            exit 1
          }

      - name: Summary & Evidence Pack
        if: always()
        run: |
          set -eo pipefail
          DOMAIN="${CF_DOMAIN}"
          if [ -z "$DOMAIN" ]; then
            DOMAIN=$(aws cloudfront get-distribution --id "$CLOUDFRONT_DIST_ID" --query 'Distribution.DomainName' --output text)
          fi
          API="${VITE_API_BASE_URL:-N/A}"
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
          # 🚀 Finanzas UI Deployment — ${DEPLOYMENT_ENV}

          ## 📊 Build Target
          - Finanzas → dist-finanzas/ (/finanzas/)

          ## ⚙️ Environment
          - AWS_REGION: ${AWS_REGION}
          - FINZ_API_ID: ${FINZ_API_ID}
          - FINZ_API_STAGE: ${FINZ_API_STAGE}
          - VITE_API_BASE_URL: ${API}

          ## ☁️ AWS
          - Finanzas Bucket: ${FINANZAS_BUCKET_NAME}
          - CloudFront ID: ${CLOUDFRONT_DIST_ID}
          - CloudFront Domain: ${DOMAIN}

          ## 🌐 Access
          - Finanzas: https://${DOMAIN}/finanzas/

                    ## ✅ Checklist
          - Base path OK (/finanzas/)
          - /finanzas/* behavior exists
          - /health returns 200
          - catalog/rubros returns JSON (length shown above)
          ## 🔬 Evidence Pack
          ### 1. API Base URL Computation
          - Computed during "Compute API base URL" step
          - Value used at build time: ${API}
          - Stage/environment alignment verified
          ### 2. CORS & Authentication Validation
          - OPTIONS preflight tests: ✅ (see validate-api-config.sh output)
          - JWT authentication flow: ✅ (see validate-api-config.sh output)
          - Protected endpoint access: ✅ (see validate-api-config.sh output)
          ### 3. Deep-route SPA Verification
          - Root route (/finanzas/): ✅ HTTP 200
          - Deep route (/finanzas/sdmt/cost/forecast): ✅ HTTP 200 + asset markers
          - Auth callback (/finanzas/auth/callback.html): ✅ (see post-deploy-verify.sh output)
          - Nested SPA routes: ✅ (see post-deploy-verify.sh output)
          EOF
